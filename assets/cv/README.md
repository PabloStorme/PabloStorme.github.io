Drop your CV here as **`cv-flor-storme.pdf`** — exactly that filename.

Two links already point at it: the button in About, and the last entry under
Contact. Rename your file to match and both start working.

If you'd rather keep a different filename, update the two `assets/cv/...`
hrefs in `index.html`.
