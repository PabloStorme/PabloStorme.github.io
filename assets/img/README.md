Drop your portrait here as **`flor.jpg`**.

- Portrait orientation, the frame crops to 4:5
- ~1000×1250px is plenty
- Keep it under ~300KB so the page stays fast

Until the file exists, the About section shows a placeholder frame instead of a
broken image. Nothing else to change — the path is already wired up.
